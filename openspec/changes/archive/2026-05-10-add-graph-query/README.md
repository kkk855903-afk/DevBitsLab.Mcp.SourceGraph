# add-graph-query

Let agents ask any question about the indexed graph by exposing a stable view layer over SQLite plus a `query_graph` tool that runs read-only SQL across all scopes, with `describe_schema` for discovery.
