# add-scope-repair-tools

Bounded retry on workspace open + agent-driven `repair_scope` and `reconcile_drift` tools so the agent can recover from transient downstream failures and from drift that the watcher missed while offline.
