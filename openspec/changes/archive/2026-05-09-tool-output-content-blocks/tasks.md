## 1. Foundation: helpers + DTO scaffolding (no behavior change yet)

- [x] 1.1 `LeafFormatter.BrandFirstText(IReadOnlyList<ContentBlock>)` and `BrandFirstText(CallToolResult)` overloads added. First user-visible (non-audience-restricted) `TextContentBlock` gets the `🌿 ` prefix; audience-restricted blocks are skipped. Idempotent and suppression-aware.
- [x] 1.2 `LeafFormatterTests` extended with 8 new scenarios covering both overloads (brands first user-visible text, skips assistant-only blocks, no-op when no text block, no-op on empty list, idempotency, suppression pass-through, CallToolResult variant).
- [x] 1.3 `ToolMetrics.TrackAsync` _additive_ generic-by-overload (not generic-by-T). Two new overloads added: `Func<Task<IReadOnlyList<ContentBlock>>>` and `Func<Task<CallToolResult>>`. _Decision: kept the existing `Func<Task<string>>` overload unchanged so all current tools continue working without modification. Tools opt into richer return types per-method. Cleaner than a single `Track<T>` with runtime dispatch and avoids type-inference surprises at call sites._
- [x] 1.4 Anonymous-type guard **NOT NEEDED — removed.** _On implementation the SDK turned out to type `CallToolResult.StructuredContent` as `JsonElement?` and `CallToolResult.Meta` as `JsonObject?`. Both reject anonymous types at **compile** time; the runtime guard would be unreachable. Updated design decision in design.md and dropped both the guard code and its corresponding test._
- [x] 1.5 `TelemetrySignalTests` extended with three scenarios for the new overloads: content-list path brands the first text block; CallToolResult path brands and preserves StructuredContent; CallToolResult with `IsError = true` records as ok=false in OTel telemetry.
- [x] 1.6 `Resources/GraphResourceUris.cs` added with `Symbol(long id)`, `File(string path)`, `Namespace(string name)` helpers — the canonical URI shape that `GraphResources.cs` will check inbound URIs against in Group 2. _Refactor of `Resources/GraphResources.cs` to use the helpers deferred to Group 2 (where the first tool actually emits resource_links and the round-trip is exercised end-to-end)._
- [x] 1.7 `Tools/Output/` namespace + `ToolOutputJsonContext` skeleton — landed in step 2.1 alongside `FindDefinitionResult` (per the deferral note in the original task — source-gen `JsonSerializerContext` requires at least one `[JsonSerializable]` to compile, so the placeholder couldn't ship empty). Both files exist at `src/DevBitsLab.Mcp.SourceGraph.Server/Tools/Output/` and have grown with each conversion group; final state covers 20 typed DTOs (`FindDefinitionResult`, `FindReferencesResult`, `SearchSymbolsResult`, `FindByAnnotationResult`, `ListCallersResult`, `ListCalleesResult`, `FindImplementationsResult`, `ListMembersResult`, `ListSymbolsInFileResult`, `NeighborhoodResult`, `ModuleSummaryResult`, `ImpactOfChangeResult`, `SemanticSearchResult`, `FindDiagnosticsResult`, `RecentChangesResult`, `ListTestsForResult`, `WhoAuthoredResult`, `ListGeneratedFilesResult`, `ListScopesResult`, `GraphStatsResult`) plus the `AudienceMetadata` helper from Group 5.
- [x] 1.8 `dotnet build` + `dotnet test` clean. **247/247 passing** (was 236 before this group; +11 new tests for the new overloads). Foundation is in place; no tool conversions yet.

## 2. Vertical slice: convert `find_definition`

- [x] 2.1 Define `FindDefinitionResult` and `FindDefinitionHit` records in `Tools/Output/FindDefinitionResult.cs`. Wrapping record structure (`{ Hits: [...] }`) so `outputSchema` can be `"type":"object"` per SDK constraint.
- [x] 2.2 Register both records on `ToolOutputJsonContext` with `[JsonSerializable]` attributes. _Single `[JsonSerializable(typeof(FindDefinitionResult))]` is sufficient — the source generator picks up nested record properties (`FindDefinitionHit`) transitively, no separate registration needed._
- [x] 2.3 Refactor `FindDefinitionAsync` in `GraphTools.cs` — return type now `Task<CallToolResult>`, decorated with `[McpServerTool(UseStructuredContent = true, OutputSchemaType = typeof(FindDefinitionResult))]`, body assembles a leading user-visible prose `TextContentBlock` + one `ResourceLinkBlock` per hit (URIs from `GraphResourceUris.Symbol`) + a trailing audience-restricted `_meta` block with `scope`, `latency_ms`, and `hits` count. `StructuredContent` is serialised via `ToolOutputJsonContext.Default.FindDefinitionResult` (snake_case wire names). _Side-quest: `ScopedExecution.RunAsync` was hard-coded to `Task<string>` — added a sibling `Task<CallToolResult>` overload (handles error / empty / single-host / multi-host fan-out) so the tool can route through the existing scope-resolution logic. No existing call sites changed; the new overload is additive, mirroring the foundation's `ToolMetrics.TrackAsync` pattern (Group 1.3)._
- [x] 2.4 Added `FindDefinitionStructuredOutputTests.cs` — six scenarios pinning: brand-marked prose lead-in, one `ResourceLinkBlock` per hit with `graph://symbol/<id>` URI shape, audience-restricted `_meta` block with `Audience = [Role.Assistant]` and `Priority < 0.5`, `structuredContent` round-trips through `ToolOutputJsonContext` with hit count matching prose, empty-result case still ships `{"hits": []}`, and every emitted resource-link id resolves through `GraphResources.GetSymbolAsync`.
- [x] 2.5 Resource-link resolution test included in 2.4's scenario set (`FindDefinition_everyEmittedResourceLinkUri_resolvesViaGraphResources`); follows each emitted URI through `GraphResources.GetSymbolAsync` and asserts the response isn't the "Symbol id N not found" / "Invalid symbol id" / "No scope" sentinel.
- [x] 2.6 `dotnet test` — **253/253 passing** (was 247 before this group; +6 new scenario tests).
- [x] 2.7 JSON-RPC roundtrip captured against `tests/fixtures/Sample.sln` via in-repo `dotnet run`. Verified: `tools/list` declares `outputSchema.type: object` with a `hits` property for `find_definition`; `tools/call` returns `content` as a 12-block array (1 leading text + 10 resource_link + 1 trailing audience-restricted text), `structuredContent` populated with 10 typed hits; snake_case field names on the wire (`file_path`, `xml_summary`); empty-result case ships `structuredContent.hits = []` and matches the no-match prose.
- [x] 2.8 **Pause for review.** Reviewed before starting Group 3. The pattern from `find_definition` holds: per-tool `CallToolResult` body building a leading prose block + resource links per row + trailing audience-restricted metadata + typed structured DTO routes cleanly through the new `ScopedExecution.RunAsync(CallToolResult)` overload and `ToolMetrics.TrackAsync(CallToolResult)`. No spec or design changes required; proceeding with the symbol-list sweep.

## 3. Sweep — symbol-list tools (template work)

Each of the following follows the same pattern as `find_definition`'s vertical slice. Group by similarity to keep diffs scannable.

- [x] 3.1 `find_references` — `FindReferencesResult { TargetFqn, TargetKind, TargetSymbolId, References: [...] }`, each `FindReferenceHit` has `kind`, `filePath`, `line`, `column`, `isGenerated`. Per-spec scenario, each row's `ResourceLinkBlock.uri` resolves to `graph://symbol/<targetId>` (the reference's symbol id is the resolved target shared across rows in our schema). _Note: the `scope?` field from the task description was omitted to stay flat-shaped for find_definition parity; scope is on the audience-restricted metadata block instead._
- [x] 3.2 `search_symbols` — `SearchSymbolsResult { Hits: [...] }`, `SearchSymbolHit` mirrors `find_definition`'s shape exactly. Resource link per hit pointing at `graph://symbol/<id>`.
- [x] 3.3 `find_by_annotation` — `FindByAnnotationResult { Hits: [...] }` where each `FindByAnnotationHit` carries `symbolId`, `fqn`, `kind`, `filePath`, `line`, `column`, plus `annotations: [...]` (full per-symbol annotation set, not just the matched one — gives downstream agents the surrounding context). Resource link per hit.
- [x] 3.4 `list_callers` and `list_callees` — `ListCallersResult { TargetFqn, TargetKind, TargetSymbolId, EdgeKind, Callers: [...] }` and the mirror for callees. Each row carries `payloadJson` (originating `edges.payload` column) when the edge had per-instance metadata. Resource link per row pointing at the calling/called symbol. Diagnostic short-circuits (unknown edge kind, no symbol match) wrap a single text block via the new `DiagnosticCallToolResult` helper so the leaf chokepoint still brand-marks the prose.
- [x] 3.5 `find_implementations` — `FindImplementationsResult { TargetFqn, TargetKind, TargetSymbolId, Implementations: [...] }`. Resource link per implementation; the abstract-base filter still runs at the prose layer so the structured row count matches what the user reads.
- [x] 3.6 `list_members` — `ListMembersResult { ContainerSymbolId, ContainerFqn, ContainerKind, Members: [...] }`. Container descriptor is at the top level (no separate metadata link); resource link per member pointing at the member's `graph://symbol/<id>`. Accessibility validation rejects unknown spellings via `DiagnosticCallToolResult`.
- [x] 3.7 `list_symbols_in_file` — `ListSymbolsInFileResult { FilePath, Symbols: [...] }`. The file path is on the top-level result; per-row resource links point at each symbol's `graph://symbol/<id>` (file link surfaced in the audience-restricted metadata block instead of as a separate content item).
- [x] 3.8 `neighborhood` — `NeighborhoodResult { SymbolId, Fqn, Kind, FilePath, Line, Column, EdgeKind, Inbound: [...], Outbound: [...] }`. Center symbol descriptor is flattened onto the result. Resource links per inbound row then per outbound row, with `Title` prefixed by the section name (`inbound:` / `outbound:`) so card UIs disambiguate.
- [x] 3.9 `module_summary` — `ModuleSummaryResult { Module, Top: [...] }`. Each `ModuleSummaryRow` carries `inDegree` + symbol descriptor. Module identifier echoed verbatim from the user-supplied namespace-or-path probe. Resource link per row.
- [x] 3.10 `impact_of_change` — `ImpactOfChangeResult { TargetFqn, TargetKind, TargetSymbolId, EdgeKind, MaxDepth, Upstream: [...] }`. Each `ImpactOfChangeRow` carries `depth` + symbol descriptor. Resource link per upstream caller.
- [x] 3.11 `semantic_search` — `SemanticSearchResult { Query, Hits: [...] }`. Each `SemanticSearchHit` carries `score` (cosine similarity in [-1, 1]) + symbol descriptor. Disabled-store and empty-result short-circuits go through `DiagnosticCallToolResult` / a structured-empty `BuildSemanticSearchResult` so empty cases still ship `{"hits": []}`. Symbol resolution is hoisted out of the prose-render loop so prose-row count and structured-array length stay in lockstep (the old code looked up `host.Store.GetSymbolByIdAsync` twice — once per row in the table path, again in the bullet path; now resolved once into a shared list).
- _Test infrastructure migration:_ `TabularRenderingTests` and `ProgressReportingTests` migrated from `string`-shaped assertions to consume `CallToolResult.Content[0].Text` via a new `ProseText` helper; assertions otherwise unchanged. **337 tests passing** (330 unit + 7 integration; was 253 after group 2.6). `dotnet build` clean.

## 4. Sweep — diagnostics, history, and singleton tools

- [x] 4.1 `find_diagnostics` — `FindDiagnosticsResult { SeverityFloor, Code, Symbol, SymbolId, Diagnostics: [...] }`. Each `FindDiagnosticsRow` has `severity` (kebab-case label), `code`, `message`, `filePath`, `line`, `column`. Per spec: **no resource link per row** — diagnostics aren't first-class graph entities. Filter inputs (severityFloor, code, symbol+symbolId) echoed at the top level so consumers can correlate the result list with the inputs that produced it.
- [x] 4.2 `recent_changes` — `RecentChangesResult { Days, Author, Changes: [...] }`. Each `RecentChangesRow` carries the symbol descriptor + `commitSha` (7-char short) + `author` + `authoredAt` (ISO-8601). Resource link per symbol pointing at `graph://symbol/<id>`. Title prefixed with `<date> <author>:` so card UIs show the change context.
- [x] 4.3 `list_tests_for` — `ListTestsForResult { TargetSymbolId, TargetFqn, TargetKind, Tests: [...] }`. Each `ListTestsForRow` has `testSymbolId`, `framework` (canonical lower-case token, "unknown" when storage didn't capture it), `testFqn`, `filePath`, `line`, `column`. Resource link per test symbol. _Side-quest: added a local `DiagnosticCallToolResult` helper to `HistoryTools` (mirror of `GraphTools`'s private helper) so the file stays independent of `GraphTools`'s internal surface._
- [x] 4.4 `who_authored` — `WhoAuthoredResult { SymbolId, Fqn, Author, Sha, AuthoredAt, BlamedLines }`. Singleton DTO (no list, but still wrapped in an object per the SDK's `"type":"object"` root constraint). All git-blame fields nullable so the "no history yet" path still ships a structured payload (with author/sha/authoredAt = null) and an agent can branch on the absence rather than parsing prose. Resource link to the resolved target symbol.
- [x] 4.5 `list_generated_files` — `ListGeneratedFilesResult { Files: [...] }`. Each `ListGeneratedFilesRow` has `filePath`, `symbolCount`. Resource link per file pointing at `graph://file/<path>` (URI-escaped) so card UIs can pull each file's outline.
- [x] 4.6 `list_scopes` — `ListScopesResult { DefaultScope, Scopes: [...] }`. Each `ListScopesRow` has `id`, `name`, `root`, `status`, `statusMessage` (degraded-only detail), `isolated`, `lastIndexedAt` (ISO-8601 or null), `projectCount`. Per spec: **no resource link per row** — scopes don't have a `graph://` URI scheme yet. _Side-quest: the existing public method was sync-string `string ListScopes(...)`; converted to `Task<CallToolResult>` and wrapped the body in `Task.FromResult` to route through the existing `TrackAsync(CallToolResult)` overload — avoids growing `ToolMetrics` with a sync-CallToolResult sibling for one tool. Empty-registry diagnostic still ships through the structured shape so consumers can probe `.scopes.length === 0` without parsing prose._
- [x] 4.7 `graph_stats` — `GraphStatsResult { Files, Symbols, References, Edges }`. Singleton DTO. Per spec: **no resource link** — the four counts are a single summary row, no list to link against.
- _Test infrastructure migration:_ `FindDiagnostics`, `ListTestsFor`, `RecentChanges` tests in `TabularRenderingTests` migrated to `ProseText` helper added in Group 3. **337 tests passing** (330 unit + 7 integration). `dotnet build` clean.

## 5. Audience-restricted metadata blocks across converted tools

- [x] 5.1 `Tools/Output/AudienceMetadata.cs` added — small `Build(scopeId, latencyMs, params extras)` helper that constructs the trailing `TextContentBlock` with `Audience = [Role.Assistant]` and `Priority = 0.2f` (`AudienceMetadata.DefaultPriority`). Standard wire shape: `_meta: scope=\`{id}\`, latency_ms={N}, key1=val1, key2=val2_`. `scopeId` is optional (null for tools where no single scope resolves, e.g. `list_scopes`); the value-formatting decision (whether to wrap a value in backticks) stays with the caller so identifier-shaped values like file paths and namespaces match the rest of the prose.
- [x] 5.2 Migrated all 20 converted-tool metadata blocks to `AudienceMetadata.Build(...)`: find_definition, find_references, search_symbols, find_by_annotation, list_callers, list_callees, find_implementations, list_members, list_symbols_in_file, neighborhood, module_summary, impact_of_change, semantic_search (Group 3) plus find_diagnostics, recent_changes, list_tests_for, who_authored, list_generated_files, list_scopes, graph_stats (Group 4). The migration audit showed all metadata fields (scope, latency, edge-kind, row counts, days/max_depth, file/module identifiers) were already in the trailing block — no leakage into user-visible prose. Audit confirmation post-migration: `grep -rn "Audience = new\[\]" src/.../Tools/` returns no matches outside `AudienceMetadata.cs` itself, and `grep -n '"_meta:' src/.../Tools/` returns only the helper's `StringBuilder` initializer. Single source of truth for metadata wire shape.
- [x] 5.3 Existing `FindDefinitionStructuredOutputTests.FindDefinition_emitsTrailingAudienceRestrictedMetadataBlock` already pins `Audience = [Role.Assistant]`, `Priority < 0.5f`, and the metadata content (scope + hit count) — written during Group 2 (vertical slice). No new test needed; the helper migration is covered by the same scenario since every converted tool's metadata block now flows through the same code path. **337 tests passing** unchanged.

## 6. Test infrastructure migration

- [x] 6.1 Audit conclusion: every test that direct-calls a converted tool was already migrated as a side-effect of Groups 3 & 4. **`FindDefinitionStructuredOutputTests`** was authored against the new shape from the start (Group 2 vertical slice). **`TabularRenderingTests`** (12 affected tests) were migrated to a `ProseText` helper that extracts the user-visible `TextContentBlock` from `CallToolResult.Content` — substring assertions on the prose lead-in stay verbatim, so per-row markdown table shape coverage is preserved. **`ProgressReportingTests`** (2 tests asserting non-empty response) were migrated to `result.Content.Should().NotBeNullOrEmpty()`. **`MultiScopeTests`** (integration) drives the wire via `harness.Client.CallToolAsync(...)` and never depends on the C# return type, so no migration needed. No test went the typed-`StructuredContent` route in this audit — every existing assertion was about user-visible prose shape; the typed-deserialise path is exercised by the new `StructuredContentInvariantTests` (6.3) instead, where it actually pays off.
- [x] 6.2 `LeafChokepointInvariantTests` extended with three new scenarios — content-list path brands the first user-visible text block while leaving `ResourceLinkBlock` items and audience-restricted text blocks untouched, `CallToolResult` path mirrors the same rule and rides `StructuredContent` through unchanged, and a suppression scenario on the content-list path verifies `LeafFormatter.Suppressed = true` ships content verbatim. The legacy single-string scenarios stay; the file now covers all three code paths the chokepoint dispatches on.
- [x] 6.3 `StructuredContentInvariantTests` added under `tests/DevBitsLab.Mcp.SourceGraph.Tests/`. **Catalog invariants (reflection):** every `[McpServerTool(UseStructuredContent = true)]` must set `OutputSchemaType`, and every type referenced via `OutputSchemaType` must be registered with `[JsonSerializable]` on `ToolOutputJsonContext` — catches drift when a future conversion forgets either half. _Side-quest: `JsonSerializableAttribute` doesn't expose its `Type` constructor argument as a public property, so the registered-types lookup goes via `GetCustomAttributesData()` and reads `ConstructorArguments[0]` directly — preserves the IL-encoded type without paying for a property we don't have._ **Runtime invariants** across a representative sample (find_definition, search_symbols, find_references, list_members, graph_stats): `StructuredContent` non-null, structured-array length matches the prose row-count declared in the lead-in (single source of truth between rendered text and typed payload), singleton DTO path works end-to-end via `graph_stats`.
- [x] 6.4 `ResourceLinkInvariantTests` added — one test per link-emitting converted tool (16 tests covering find_definition, find_references, search_symbols, find_by_annotation, list_callers, list_callees, find_implementations, list_members, list_symbols_in_file, neighborhood, module_summary, impact_of_change, list_generated_files, list_tests_for, who_authored, recent_changes). Each test drives the tool against `Sample.sln`, walks every `ResourceLinkBlock.uri`, and confirms it dereferences cleanly through `Resources/GraphResources` without producing a `# Symbol id N not found` / `# Invalid symbol id` / `# No scope` sentinel. Handles all three URI shapes — `graph://symbol/<id>` via `GetSymbolAsync`, `graph://file/<path>` via `GetFileOutlineAsync`, `graph://namespace/<name>` via `GetNamespaceSummaryAsync` — so any future tool's link emission is covered without a new test if the URI matches one of those shapes. _Side-quest: `recent_changes` needed seeded `SymbolHistory` rows because the test fixture doesn't run the history pipeline; without the seed the tool returns zero rows and the test silently no-ops. Same approach `TabularRenderingTests.RecentChanges_multipleHits_rendersTable` uses._ **All 363 tests passing** (was 337 — the new invariant tests cold-index Sample.sln per-test the same way `FindDefinitionStructuredOutputTests` does, so per-class wall-time is dominated by indexing).

## 7. Documentation

- [x] 7.1 `README.md` — added "Structured output and resource links" section between the MCP-tools list and the resource-templates table, plus a TOC entry. Section explains the two-view contract (renderable prose in `content` + typed `structuredContent`), the trailing audience-restricted metadata block, and the older-client fallback story (clients that don't recognise `structuredContent` / `resource_link` / `audience` annotations still get a usable response). Sub-section "`graph://` URI scheme" tabulates the three URI shapes (`symbol`, `file`, `namespace`) and cross-refs the existing Resource templates section so the deeper dive doesn't duplicate. Section closes with the downstream-author guidance: read `result.structuredContent.<list-key>[i]` directly; plugin authors who want the same shape return `CallToolResult` from their handler.
- [x] 7.2 `CLAUDE.md` — one-paragraph note added between the green-leaf brand-mark blurb and the JSONL audit log paragraph: "Built-in `find_*` / `list_*` / `search_*` tools ship typed `structuredContent` (snake-case fields, `outputSchema` declared on `tools/list`) alongside the prose, so agents chaining tool calls can consume `result.structuredContent` directly without re-parsing markdown — see README's 'Structured output and resource links' for the shape and a worked example." Future Claude sessions opening this repo see the structured-output contract surfaced in their default context.
- [x] 7.3 `README.md` — sample `find_definition` payload added inline as `### Sample find_definition payload` subsection inside the new section. Shows the full multi-content shape against the Sample fixture: leaf-prefixed prose first, one `resource_link` per hit (with `name`/`title`/`description`/`mimeType` populated for renderer cards), trailing `audience: ["assistant"]` metadata block, and the parallel `structuredContent.hits[]` array with snake-case field names matching the wire schema. Trailing prose calls out the invariant that `structuredContent.hits.length == resource_link.length == prose row count`.

## 8. Verification

- [x] 8.1 `dotnet build` clean. **0 warnings, 0 errors.** Build elapsed: 2.07s.
- [x] 8.2 `dotnet test` — full suite green. **363/363 passing** (356 unit + 7 integration). With `--no-build` the suite runs in ~45s.
- [x] 8.3 Substituted manual UI eyeball with a programmatic JSON-RPC stdio roundtrip (`/tmp/sourcegraph-roundtrip.py`) against `tests/fixtures/Sample.sln` via `dotnet run --no-build`. Captured wire-level evidence: `tools/list` declares `outputSchema` with `"type": "object"` for **20/20 converted tools**; `tools/call` for `find_definition('Calculator')` returns 10 hits + 10 `resource_link` items + 1 audience-restricted text block (`scope=`default``, `latency_ms=…`) + `structuredContent.hits` of length 10; `find_references('Calculator.Add')` returns 12 `resource_link` items all with `graph://symbol/<top.Id>` URIs (per-row link-to-target shape from Group 3); `list_generated_files` emits `graph://file/<path>` URIs; empty-case `find_definition('ZzzNonexistent')` ships `structuredContent.hits = []`. **Schema-payload casing alignment fixed in same session:** the SDK's `JsonSchemaExporter` doesn't pick up `ToolOutputJsonContext`'s `SnakeCaseLower` property naming policy, so without overrides the schema declared on `tools/list` used C#-default (camelCase: `filePath`, `xmlSummary`) while the payload used snake_case (`file_path`, `xml_summary`) — strict-validating clients would have rejected this. Resolved by adding `[property: JsonPropertyName("snake_name")]` to every multi-word field across all 19 DTOs (GraphStatsResult is single-word-only and needs no overrides); the attribute is honored by both source-gen serialization and the schema exporter, so the wire shape converges. Roundtrip script now asserts `find_definition.hits[].file_path` / `.xml_summary` appear in the schema (and the camelCase variants do not). The VS Code Insiders Claude-extension UI render (cards + audience suppression) was not driven in this session — left as a manual smoke when the extension's UI hooks are stable.
- [x] 8.4 Token-cost smoke captured during the 8.3 roundtrip against `Sample.sln` (with `--no-leaf --no-embeddings --no-history` so byte counts reflect substantive payload, not chrome): `find_definition('Calculator')` 10 hits → **11,901B**; `find_references('Calculator.Add')` 12 refs → **11,445B**; `search_symbols('Add')` 7 hits → **7,920B**. Without a pre-conversion reference (would require checking out an older commit) these are directional rather than a delta, but the per-row breakdown is consistent with the spec's "~+30 tokens for the structured payload" budget: each `ResourceLinkBlock` adds ~80–120B (URI + name + title + description + mimeType), the trailing audience-restricted block adds ~80–120B, and the `structuredContent` array roughly mirrors the prose row content one-for-one. The savings claim ("agents skip markdown re-parsing") is unmeasurable in a single session — that's a longitudinal observation across many tool-call chains; deferred to ongoing usage telemetry rather than this verification step.
- [x] 8.5 `openspec validate tool-output-content-blocks --strict` ⇒ **"Change 'tool-output-content-blocks' is valid"**.

## 9. Spec sync (archive)

- [x] 9.1 `openspec archive tool-output-content-blocks --yes` — succeeded with the documented 4 ADDED requirements (`Multi-content tool responses`, `Structured content output`, `Resource-link content items`, `Audience-restricted metadata content blocks`) and 1 MODIFIED requirement (`Tool response brand mark`) applied to `openspec/specs/mcp-tools/spec.md` cleanly. Live spec grew from 454 to 536 lines, requirement count from 30 to 35. Change directory moved to `openspec/changes/archive/2026-05-09-tool-output-content-blocks/`. _The CLI emitted a non-blocking proposal-warning ("Why section should not exceed 1000 characters") — left as-is since the proposal section is comprehensive by design and the warning is a soft guideline rather than a validation failure._
