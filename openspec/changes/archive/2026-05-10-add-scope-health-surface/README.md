# add-scope-health-surface

Detect and surface scope health (orphan DBs, stuck `indexing` rows) on boot; expose `verify_scope` and a heal-event log so subsequent self-healing changes have an observable substrate.
