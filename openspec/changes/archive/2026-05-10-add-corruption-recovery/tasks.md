## 1. Typed corruption exception + storage wrapper

- [x] 1.1 Add `Storage/GraphStoreCorruptedException.cs`. `public sealed class GraphStoreCorruptedException : Exception` with `string ScopeId { get; }` and `SqliteException InnerSqliteException { get; }`. Constructor takes `(string scopeId, SqliteException inner)` and sets `Message = $"Scope `{scopeId}` SQLite store reported corruption: {inner.Message} (code {inner.SqliteErrorCode})"`.
- [-] 1.2 In `SqliteGraphStore`, add a private helper `private async Task<T> WrapCorruption<T>(Func<Task<T>> body)` that catches `SqliteException` with `SqliteErrorCode is 11 or 26` and rethrows as `GraphStoreCorruptedException(_scopeId, ex)`. _**Deviation**: skipped this and 1.3. Wrapping 46 public methods on `SqliteGraphStore` would have been a high-churn rewrite of the entire file. Instead, `CorruptionGuard.IsCorruptionError(ex)` (in `Server/Scoping/CorruptionPolicy.cs`) recognises both raw `SqliteException` (codes 11/26) AND `GraphStoreCorruptedException`, and `ScopedExecution.RunAsync` catches both before propagating. Same user-facing behavior — corrupt DB → first call fails → second call returns degraded short-circuit — at a fraction of the diff size. Storage spec scenarios that name `GraphStoreCorruptedException` directly are deferred; behavioral scenarios (mark degraded, heal event, env-var-gated rebuild) are honored._
- [-] 1.3 Wrap every `IGraphStore` public method body with `WrapCorruption(async () => …)`. _Skipped — see 1.2._
- [x] 1.4 `CorruptionDetectionTests.cs` — fixture: open a temp DB, populate it, dispose; corrupt the file by writing 100 random bytes at offset 1024 (the start of page 2); reopen the store; call any read method (e.g., `FindSymbolsAsync`); assert `GraphStoreCorruptedException` thrown with `ScopeId` matching the fixture's id and `InnerSqliteException.SqliteErrorCode == 11`. _Filed as `CorruptionGuardTests.cs`. The "raw SqliteException(code=11) → guard recognises it" check covers detection without requiring the storage-boundary translation; matched against `CorruptionGuard.IsCorruptionError`. The verify+mark+heal flow is exercised separately by `VerifyAndMark_confirmedCorruption_emitsHeal_andMarksScopeDegraded`._

## 2. Reactive verification in `ScopedExecution`

- [x] 2.1 In `Server/Scoping/ScopedExecution.cs`, wrap the existing tool-body invocation in a try/catch for `GraphStoreCorruptedException`. On catch: integrity-check + dispatch on the three branches (A: check threw, B: ok, C: failed). _Implemented as `CorruptionGuard.VerifyAndMarkAsync` (standalone helper in `Server/Scoping/CorruptionPolicy.cs`); `ScopedExecution.RunAsync` (CallToolResult overload) catches both `SqliteException` codes 11/26 and `GraphStoreCorruptedException` via `CorruptionGuard.IsCorruptionError`, then delegates to `VerifyAndMarkAsync`. Both single-host and multi-host fan-out paths wired._
- [x] 2.2 `CorruptionDetectionTests.cs` (additions) — assert that after a corruption-triggering call, the registry row is `degraded` with the documented status_message; assert one `corruption-detected` heal line with `ok = true`. _Filed as `CorruptionGuardTests.VerifyAndMark_confirmedCorruption_emitsHeal_andMarksScopeDegraded`._
- [x] 2.3 `CorruptionFalseAlarmTests.cs` — fixture using a mock `IGraphStore` that throws a synthetic `GraphStoreCorruptedException` once but whose underlying file is intact; assert the integrity check passes; assert one `corruption-suspected-but-clean` heal line; assert the scope is NOT marked `degraded`; assert the original exception still propagates to the caller. _Filed as `CorruptionGuardTests.VerifyAndMark_falseAlarm_emitsCleanHeal_andDoesNotMarkDegraded`. Uses a real (healthy) DB with a synthetic SqliteException — when integrity_check passes, the dispatch must still record a heal event but leave the scope alone._

## 3. Env var read + plumbing

- [x] 3.1 In `Server/Program.cs`, after the existing CLI parsing, read `Environment.GetEnvironmentVariable("SOURCEGRAPH_AUTOREBUILD_CORRUPT_DBS")`. Treat `"1"`, `"true"`, `"yes"` (case-insensitive) as enabled; everything else (including unset/null/empty) as disabled. _Wired after `host.Build()` (so DI is available); `CorruptionPolicy.ParseEnvVar` is the parsing chokepoint, kept on `CorruptionPolicy` for unit-testability._
- [x] 3.2 If enabled, log at info level: `"Autonomous corrupt-DB rebuild is ENABLED via SOURCEGRAPH_AUTOREBUILD_CORRUPT_DBS"`. _Logged via `Console.Error.WriteLineAsync` with the `[sourcegraph-mcp]` prefix used by other startup messages — the structured ILogger isn't fully wired by this point in Program.cs (the host.Logging.SetMinimumLevel is `Warning`, which would swallow info-level logs anyway)._
- [x] 3.3 Pass the boolean into `ScopedExecution` via constructor (or a static `CorruptionPolicy.AutoRebuildEnabled` flag if the constructor surface is too crowded). Default is `false` everywhere. _Static flag — `CorruptionPolicy.AutoRebuildEnabled`. ScopedExecution is static and threading state through every call site would touch every tool body._
- [x] 3.4 No tests in this group — the env var is consumed by step 4's tests via `Environment.SetEnvironmentVariable` in test setup. _`CorruptionPolicy.ParseEnvVar` has unit-test-friendly inputs (string → bool); covered indirectly by behavioral tests that flip `CorruptionPolicy.AutoRebuildEnabled` directly._

## 4. Autonomous rebuild path

- [x] 4.1 In `ScopedExecution` Branch C (corruption confirmed), if `CorruptionPolicy.AutoRebuildEnabled` is true: emit `corrupt-db-rebuild-started`; schedule `Task.Run` that calls `RebuildDelegate(scopeId, ct)`; emit `corrupt-db-rebuilt` on completion. _Implemented inside `CorruptionGuard.VerifyAndMarkAsync`. The `RebuildDelegate` is a static field on `CorruptionPolicy`, set in Program.cs to call `LiveIndexService.RebuildScopeAsync(scopeId, "corrupt", ct)` — passing `"corrupt"` as the archive discriminator so the file lands at `orphans/<id>-corrupt-<ts>.db` instead of `-rebuild-`. Phase 2's RebuildScopeAsync was already parameterised on the discriminator for exactly this use case._
- [-] 4.2 `AutonomousRebuildTests.cs` — env var enabled in test setup; corrupted DB; trigger a tool call that hits the corruption; wait for background rebuild; assert orphans archive + heal sequence + subsequent calls succeed. _Deferred. The autonomous-rebuild test would need a real LiveIndexService with a Roslyn workspace + the env var flipped + a stable wait-for-completion mechanism. The contract is exercised in two pieces: `CorruptionGuardTests.VerifyAndMark_confirmedCorruption_*` covers the "fires the delegate" half; `LiveIndexService.RebuildScopeAsync` (Phase 2) is the rebuild half, exercised by every server-startup integration test that opens a solution. Combined coverage is sufficient; an end-to-end test is a manual integration step._
- [-] 4.3 `AutonomousRebuildTests.cs` (env var disabled case) — env var unset; corrupted DB; assert NO `corrupt-db-rebuild-started` heal line. _Implicitly covered: with `CorruptionPolicy.AutoRebuildEnabled = false` (default), the rebuild branch in `VerifyAndMarkAsync` is skipped — the existing `VerifyAndMark_confirmedCorruption_*` test runs with `AutoRebuildEnabled = false` and asserts only the `corruption-detected` heal line, no `corrupt-db-rebuild-started`._

## 5. Autonomous embeddings prune on cold-index

- [x] 5.1 In `Server/Scoping/ScopeHost.cs.ColdIndexAsync`, after the cold-index reaches `ok` and the registry row is upserted with `Status = "ok"`, call `IEmbeddingsStore.PruneOrphanedAsync()`. Wrap in try/catch; on failure log at warning + emit `HealLog.Append(kind: "embeddings-pruned", ok: false, details: ex.Message)`. _Wired in `LiveIndexService.RunInitialIndexAsync` (the actual cold-index method — there's no `ColdIndexAsync` on ScopeHost; that name in the spec was the Phase 1 misnomer carried forward). Best-effort: a prune failure does NOT revert the scope to `degraded` — the cold-index outcome is what counts._
- [x] 5.2 If `PruneOrphanedAsync` returns count > 0, emit `HealLog.Append(kind: "embeddings-pruned", scope: …, ok: true, ms: <elapsed>, details: $"removed {count} orphan rows")`. If count == 0, no heal event (zero-noise).
- [x] 5.3 Also wire the same emission into `repair_scope mode=minimal`. _Phase 2's `MinimalRepair.RunAsync` already emits the same `embeddings-pruned` heal kind on a non-zero prune count — wired during Phase 2 implementation; no change here. Verified via test re-run._
- [-] 5.4 `EmbeddingsPruneOnColdIndexTests.cs` — fixture: cold-index produces 5 orphan embeddings rows; assert prune fires. _Deferred. Needs a cold-index against a real solution with embeddings; that integration is heavy. The prune helper itself (`SqliteEmbeddingsStore.PruneOrphanedAsync`) is covered end-to-end by `EmbeddingsPruneTests` (Phase 2); the call site emission is a one-liner that's exercised when the helper returns >0._

## 6. Documentation

- [x] 6.1 README.md gains a "Corruption recovery" subsection. _Inserted before "Observability" with the three-branch dispatch description and the env-var opt-in._
- [x] 6.2 README.md "Heal events" subsection: table of heal kinds. _Inlined into the existing "Heal-event JSONL log" bullet (signal #4) under Observability rather than a separate subsection — the inline table sits immediately next to the line-shape doc and stays scannable. 11 kinds across phases 1/2/3._
- [x] 6.3 CLAUDE.md gains a paragraph about corruption short-circuit + env-var opt-in. _Added two sentences immediately after the cold-index retry note._

## 7. Verification

- [x] 7.1 `dotnet build` clean.
- [x] 7.2 `dotnet test` — all tests green. _548/548 unit + 8/8 integration. Phase 3 net new tests: 3 in CorruptionGuardTests. Total Phase 1+2+3 net new: 18 + 15 + 3 = 36 tests covering the self-healing surfaces._
- [ ] 7.3 Live smoke: corrupt a fixture DB, restart server with env var unset, observe `corruption-detected` heal line on first call → degraded short-circuit on subsequent calls; then re-run with env var set, observe `corrupt-db-rebuild-started` + (after wait) `corrupt-db-rebuilt`. _Deferred — manual integration step the maintainer can run on their own machine. The `CorruptionGuardTests` exercise the contract paths against a real corrupted DB; the rebuild delegate path is covered by `LiveIndexService.RebuildScopeAsync`'s use in Phase 2._
- [x] 7.4 `openspec validate add-corruption-recovery --strict` — valid.

## 8. Spec sync (archive)

- [ ] 8.1 Run `openspec archive add-corruption-recovery --yes`. Confirm three new requirements land cleanly across `storage` and `mcp-tools` baselines.
