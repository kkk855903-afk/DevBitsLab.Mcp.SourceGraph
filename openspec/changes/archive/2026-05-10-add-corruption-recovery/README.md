# add-corruption-recovery

Detect SQLite corruption on suspicion, autonomously prune orphan embeddings on cold-index completion, and gate autonomous DB rebuild behind an explicit opt-in env var so the default stance remains "detect-and-surface, don't silently destroy".
